import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'el-tiempo',
  webDir: 'dist/el-tiempo',
  bundledWebRuntime: false
};

export default config;
